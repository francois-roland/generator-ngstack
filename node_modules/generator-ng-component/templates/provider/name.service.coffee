'use strict'

(->

  <%= cameledName %> = ($injector) ->

    # Private variables
    salutation = 'Hello'

    # Private constructor
    greet = ->
      salutation


    return {

      # Public API for configuration
      setSalutation = (s) ->
        salutation = s

      # Method for instantiating
      $get = ($injector) ->
        new Greeter()
        # this is the only place and method to inject dependencies in a provider
        # $q is just an exemple, you can inject anything else
        $q = $injector.get('$q')
    }


  <%= cameledName %>
    .$inject = ['$injector']

  angular
    .module '<%= scriptAppName %>'
    .provider '<%= cameledName %>Provider', <%= cameledName %>


)()
